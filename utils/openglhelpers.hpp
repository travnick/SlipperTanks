#pragma once

namespace utils
{
void enableVertaxAttribPointer(GLuint index,
                               GLsizei size,
                               GLenum type,
                               GLboolean normalized,
                               GLsizei stride,
                               GLuint offset);
}
